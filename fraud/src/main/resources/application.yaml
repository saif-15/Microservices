spring:
  application:
    name: Fraud
  datasource:
    username: 'root'
    url: jdbc:postgresql://localhost:5432/fraud
    password: 'root'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: 'true'
